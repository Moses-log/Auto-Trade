# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Settings / env
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# Alpaca trading SDK (official)
alpaca-py>=0.28.0

# HTTP client (used for notifications)
httpx>=0.27.0

# Retry logic
tenacity>=8.3.0

# Ed25519 signature verification for Discord interactions
cryptography>=42.0.0

# Chart generation for P&L reports
matplotlib>=3.8.0

# Task scheduler for P&L tracking
APScheduler>=3.10,<4

# Timezone support (required by alpaca.data)
pytz>=2024.1

# Market data (free, no subscription required)
yfinance>=0.2.40
# ── Dev / test (not needed in production) ────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0   # also used by TestClient
